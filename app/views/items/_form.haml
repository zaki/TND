= simple_form_for item do |f|

  = f.input :name
  = f.input :description
  = f.input :price
  = f.input :expires_at, :as=>:date
  = f.input :public, :as=>:select
  = f.file_field :photo

  = f.submit
